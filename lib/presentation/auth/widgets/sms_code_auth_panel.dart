import 'package:flutter/material.dart';

class SMSCodeAuthPanel extends StatelessWidget {
  const SMSCodeAuthPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('$SMSCodeAuthPanel'),
    );
  }
}
