import 'package:flutter/material.dart';

class PhoneNumberAuthPanel extends StatelessWidget {
  const PhoneNumberAuthPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('$PhoneNumberAuthPanel'),
    );
  }
}
